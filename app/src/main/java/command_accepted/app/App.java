/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package command_accepted.app;

import command_accepted.list.LinkedList;

import static command_accepted.utilities.StringUtils.join;
import static command_accepted.utilities.StringUtils.split;
import static command_accepted.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
